import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App initializes correctly', (WidgetTester tester) async {
    // Test de base — sera enrichi avec les vrais tests.
    expect(1 + 1, equals(2));
  });
}
